### Dependency issue

I encountered a dependency conflict while setting up the frontend. I resolved it by using a compatible package version and committing `package-lock.json` to keep the dependency versions consistent across environments.

AI suggested possible fixes, but I confirmed the solution by testing the project locally.