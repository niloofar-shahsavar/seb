# Frontend

React/Vite dashboard for the Portfolio Bond Compliance Monitoring prototype.

The frontend reads monitoring results from the FastAPI backend at:

`http://localhost:8000/api/daily-run`

For setup and project documentation, see the main README in the repository root.